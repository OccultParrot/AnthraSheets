export default function Header() {
    return (
        <header className="sticky top-0 z-50 bg-transparent p-4 flex items-center justify-between text-text">
            <button className="bg-blurple p-2 rounded-2xl">
                Import
            </button>
        </header>
    )
}